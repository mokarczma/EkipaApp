@*@model Ekipa.Models.ViewModel.ImageVM

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>ImageVM</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.Id)

        <div class="form-group">
            <img class="img-responsive" src="@Url.Content(Model.Link)" />
        </div>

        <div class="form-group">
            <p> Usuń: @Html.CheckBoxFor(model => model.IsDelete, new { @checked = "checked" }); </p>
        </div>

        <div class="form-group">
            <p> Ustaw jako zdjęcie głowne: @Html.CheckBoxFor(model => model.MainPicture, new { @checked = "checked" }); </p>

        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Powrót", "IndexCompany","Account")
</div>*@
